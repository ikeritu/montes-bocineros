# INFORME V4.11I.1 — Ajustes de producción tras auditoría

## Objetivo

Aplicar cuatro correcciones detectadas tras revisar la web publicada:

1. Añadir al hero de `montes.html` la animación de relieve usada en el `index`.
2. Retirar de `montes.html` el desplegable visible “Ver mapa estático / versión simple”.
3. Normalizar en `veredicto.html` la ficha “Matiz clave V4.2A” para que use formato de tarjeta hero y no muestre nomenclatura interna.
4. Unificar el menú “Profundizar” y la cabecera en todas las páginas.

## Criterio aplicado

La animación de relieve se reutiliza como recurso visual simbólico. No se presenta como simulación histórica, acústica ni topográfica exacta.

El menú se toma del `index.html` actual como cabecera canónica y se replica en todos los HTML.
